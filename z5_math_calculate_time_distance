/*Napisati program kojim će se izračunati 20 uzastopnih vrijednosti brzine i pređenog puta objekta
u slobodnom padu u razmaku od po 3 sekunde. Pri računanju potrebno je ispisati na ekran uzastopno brzinu i pređeni put 
zajedno za svaki period od 3 sekunde. Obratiti pažnju da se koriste ispravni tipovi podataka. 
Formula za brzinu slobodnog pada je v=g*t; g=9.81. Formula za pređeni put pri slobodnom padu je s=(g/2)*pow(t,2)*/

#include <iostream>
#include <math.h>
using namespace std;

int main()
{
	float v = 0, s=0;
	float g=9.81;
	int t = 0;
	for (int i = 1; i <= 20; i++)
	{
		t = i * 3;
		v = g * t;
		s = (g/2)*pow(t, 2);
		cout << "Situacija u " << t << "-oj sekundi je: " << endl;
		cout << "Brzina: " << v << endl;
		cout << "Predjeni put: " << s<<endl;
	}

    system("pause>0");
    return 0;
}
